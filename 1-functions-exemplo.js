function exemplo() {
    var exemplo = 'exemplo';
    if (exemplo) {
        console.log('A variável exemplo não é necessariamente true mas entrou no if. Alguém sabe pq? ', exemplo);
    }
}

exemplo(); // executa a função